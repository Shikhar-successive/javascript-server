import { makeStyles } from '@material-ui/core/styles';

export const theme = makeStyles({
  root: {
    fontFamily: '"Comic Sans MS","cursive","sans-sarif"',
    htmlFontSize: 10,
  },
});
export default theme;
